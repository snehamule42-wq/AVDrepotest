name: Run python job
on: 
    push:
        branches:
        - main

jobs:
    ci-pipeline:
        runs-on: ubuntu-latest
        steps:
        -name: Checkout Code
         uses: actions/checkout@v2

        -name: Print Message
         run: echo 'Welcome to AVD'